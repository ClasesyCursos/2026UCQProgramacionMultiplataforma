function DemoHeader() {
  return (
    <header className="demo-header">
      <h1>Guia Didactica de React</h1>
      <p>
        Ejemplos integrados en una sola pagina: desde UI = f(State) hasta
        useEffect y listas.
      </p>
    </header>
  )
}

export default DemoHeader
